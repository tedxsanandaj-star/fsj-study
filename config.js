/* ══════════════════════════════════════════════════════════════════
   config.js — تنها فایلی که شما باید ویرایش کنید

   ۵ مقدار زیر را پر کنید. بقیه را دست نزنید.
   بعد از ویرایش، فایل را ذخیره کنید و در GitHub بارگذاری نمایید.
   ══════════════════════════════════════════════════════════════════ */

window.CFG = {

  /* ───── ۱. آدرس ذخیره داده (از Google Apps Script) ─────
     بعد از Deploy کردن اسکریپت، آدرسی که می‌گیرید را اینجا بگذارید.
     باید با /exec تمام شود.                                       */
  API_URL: 'PASTE_YOUR_APPS_SCRIPT_URL_HERE',

  /* ───── ۲. محل تصاویر چهره ─────
     تصاویر داخل همین پوشه هستند؛ این مقدار را تغییر ندهید.       */
  FACES_BASE: 'faces/',

  /* ───── ۳. محل دو ویدیو ─────
     اگر ویدیوها را در پوشه video همین مخزن گذاشته‌اید، این مقادیر
     درست هستند و نیازی به تغییر ندارند.
     اگر ویدیو را جای دیگری میزبانی کرده‌اید، لینک مستقیم mp4 را
     اینجا بگذارید (نه لینک صفحه تماشا).                           */
  VIDEO_A: 'video/A.mp4',
  VIDEO_B: 'video/B.mp4',

  /* آدرس پشتیبان — اگر لینک بالا باز نشد، این یکی امتحان می‌شود.
     خالی گذاشتن اشکالی ندارد.                                     */
  VIDEO_A_ALT: '',
  VIDEO_B_ALT: '',

  /* VIDEO_A = ویدیوی مداخله (مهربانی)  → گروه ۱
     VIDEO_B = ویدیوی کنترل (طبیعت)      → گروه ۰
     اگر جای این دو را اشتباه بگذارید، کل نتیجه مطالعه وارونه می‌شود.
     بعد از تنظیم، حتماً صفحه check.html را باز کنید تا بررسی شود.   */

  /* ───── ۴. آدرس عمومی مطالعه ─────
     برای ساخت لینک دعوت و بررسی سلامت. مثال:
     'https://myname.github.io/fsj-study/'                          */
  SITE_BASE: 'https://YOUR_USERNAME.github.io/fsj-study/',

  /* ───── ۵. ایمیل تماس برای شرکت‌کنندگان ───── */
  CONTACT_EMAIL: 'your.email@example.com',


  /* ══════════ از اینجا به بعد را فقط در صورت نیاز تغییر دهید ══════════ */

  /* مهلت پاسخ (میلی‌ثانیه). صفر = بدون مهلت.
     این مقادیر عمداً سخاوتمندانه‌اند تا کسی دچار استرس نشود.        */
  TIMEOUT_FSJ:        25000,   // هر کوشش مقایسه چهره
  TIMEOUT_TRUST:      45000,   // هر سناریوی بازی سرمایه‌گذاری
  TIMEOUT_STA:        12000,   // هر ارزیابی سریع
  TIMEOUT_QUESTION:       0,   // پرسشنامه‌ها: بدون محدودیت

  /* زمان‌بندی نسخه «تأخیردار» تکلیف چهره (دستکاری آزمایشی) */
  DELAY_TARGET_MS:     1200,   // مدت نمایش چهره مرجع
  DELAY_MASK_MS:        500,   // مدت نمایش ماسک بصری

  /* نمایش چهره در ارزیابی سریع اعتماد */
  STA_FACE_MS:          500,

  /* حداقل درصد ویدیو که باید تماشا شود تا دکمه ادامه فعال شود */
  VIDEO_MIN_WATCH:      0.97,

  /* ارسال داده: چند بار تلاش مجدد و با چه فاصله‌ای (میلی‌ثانیه) */
  SEND_RETRIES:             5,
  SEND_RETRY_DELAY:      2500,

  /* اگر true باشد، تکلیف ارزیابی سریع اعتماد (STA) اجرا می‌شود */
  ENABLE_STA:            true,

  /* حالت آزمایش: داده واقعی ثبت نمی‌شود و همه مراحل سریع رد می‌شوند.
     قبل از انتشار حتماً false کنید.                                */
  DEBUG: false
};
